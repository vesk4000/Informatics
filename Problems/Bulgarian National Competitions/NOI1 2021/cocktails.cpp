#include <bits/stdc++.h>
using namespace std;

/// lazy seg tree, where if a every child of a node has the same value, then the node has that value, else it is -1
/// hash by value = value * primes[x[i]] % MOD; 

int main() {

}