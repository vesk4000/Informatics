// #: #dp
#include <iostream>
#include <string>
using namespace std;

class Solution {
public:
    bool isMatch(string s, string p) {
        return true;
    }
};

int main() {
	string s, p;
	cin >> s >> p;
	Solution sol;
	cout << sol.isMatch(s, p) << endl;
	return 0;
}